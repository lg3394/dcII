// bar_chart.js
d3.csv('data/energy_consumption_per_task.csv').then(data => {
  // Set SVG dimensions, scales, and axes

  // Map data to bars
  // On hover, show tooltip with task and average Wh
  // Add axis labels
});
